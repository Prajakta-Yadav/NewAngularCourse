// src/utils/math.js
const add = (a, b) => a + b;
const subtract = (a, b) => a - b;
const multiply = (a, b) => a * b;

// Everything is wrapped in ONE object
export default { add, subtract, multiply };