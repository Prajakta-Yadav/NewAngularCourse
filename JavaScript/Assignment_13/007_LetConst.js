//console.log(b);  //reference error
let b = 20;

console.log(c);    //reference error
const c =30;


//let and const are hoisted, but they stay in Temporal Dead Zone (TDZ) until declaration line is reached.